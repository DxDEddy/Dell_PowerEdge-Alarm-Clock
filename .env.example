# iDRAC passwords — one per server, referenced in docker-compose.yml as ${VAR_NAME}
IDRAC_PASSWORD=changeme

# For multi-server setups, use unique variable names per server:
# R630_IDRAC_PASSWORD=changeme
# R520_IDRAC_PASSWORD=changeme

# Alarm check hosts — comma-separated hostnames or IPs
# The alarm is dismissed when ANY of these respond to ping (user is awake)
# Single host:
ALARM_CHECK_HOSTS=workpc.local
# Multiple hosts (OR logic — any one wakes you):
# ALARM_CHECK_HOSTS=workpc.local,phone.local,laptop.local
